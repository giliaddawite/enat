<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- allowBackup stays off so Auto Backup can never ship cached email digests or
         tokens to Drive once the Room cache (TICKET-203) exists. Re-enable only with
         explicit dataExtractionRules that exclude the database and any credentials.
         usesCleartextTraffic is defense-in-depth: every backend URL is https. -->
    <application
        android:name=".EnatApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Enat"
        android:usesCleartextTraffic="false">

        <!-- Crash reporting never starts on its own: EnatApplication enables it
             explicitly, and only in release builds. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
